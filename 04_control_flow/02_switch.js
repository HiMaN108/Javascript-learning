//NOTE - switch stataement

const month="feb"
switch (month) {
    case "jan":
        console.log(`jan 1st month`);
        break;
    case "feb":
        console.log(`2nd month`);
        break;

    default:
        console.log("default case match");
        break;
}
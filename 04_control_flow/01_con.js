//NOTE - if statement is true then it will exectued basied on condition okay

const isUserLogged = true;
if(isUserLogged){
            console.log(`user is login`);
}else{
    console.log(`user nor loggedIn`);
}

console.log(`this will always exectued`)  //NOTE - this will execetd alway because it is out of the scope of if-else statement

//NOTE - some comparison operator >, <, <=, >= , == , != , ===(check the datatype also)

//NOTE - implicit if statement

const account=400
 if(account==400) console.log(`Account balacce is ${account}`)
